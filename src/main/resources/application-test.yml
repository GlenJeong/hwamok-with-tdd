spring:
  spring:
    profiles: prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hwamok-tdd
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

cloud:
  aws:
    credentials:
      access-key: AKIA5CD3QQSFBJFUA6N7
      secret-key: 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
    region:
      static: ap-northeast-2

s3:
  bucket: hwamok3

jwt:
  secretKey: ${hwamok.jwt.secret.key:123}
# Elastic Beanstalk > #Environments > #dev-hwamok-env > 구성 > 환경 속성
# Elastic Beanstalk 환경 생성하면서 설정해줌

